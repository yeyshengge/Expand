server:
  port: 8080
eureka:
  client:
    #是否将自己注册到eureka
    register-with-eureka: false
    #是否去eureka拉取服务
    fetch-registry: false
    service-url:
      defaultZone: http://127.0.0.1:${server.port}/eureka/